name: Test rag-loader

on:
  push:
    paths:
      - "python/rag-loader/**"
      - "python/lib-schemas/**"
  pull_request:
    paths:
      - "python/rag-loader/**"
      - "python/lib-schemas/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: python/rag-loader
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --group dev

      - name: Run pre-commit
        uses: pre-commit/action@v3.0.1
        with:
          extra_args: --files python/rag-loader/**/*

      - name: Run tests
        run: uv run pytest
